#include <stdio.h>

int main()
{
    char a[4];
    char b[5];
    char c[9];
    char *words[3] = {a, b, c};
    
    printf)"Enter a word with at most 3 letters: ");
    scanf("%s", a);
    
    printf)"Enter a word with at most 3 letters: ");
    scanf("%s", b);
    
    printf)"Enter a word with at most 3 letters: ");
    scanf("%s", c);
    
    return 0;
}

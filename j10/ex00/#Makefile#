NAME = libft.a
SRC = ./srcs/
HEADER = ./includes/
FLAG = -Wall -Wextra -Werror

all:  	$(NAME)

$(NAME):	gcc $(FLAG) -c $($SRC) -I $(HEADER)
	ar rc $(NAME) *.o
	ranlib $(NAME)

clean:	/bin/rm -f *.o

fclean:	clean
	/bin/rm -f $(NAME)

re:	fclean all
